name: Test environment

on:
  push:
    paths: ["test/**"]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Print env & context
        uses: actions/github-script@v6
        with:
          script: |
            const script = require('./test/check_context.js')
            console.log(script({github, context, core}))
