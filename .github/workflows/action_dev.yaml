name: action_dev

on:
  workflow_dispatch:

jobs:
  action_dev:
    name: action_dev
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: choshsh/action-config-env@v0.0.4
        with:
          branch: ${{ github.ref_name }}
          branch-prod: master

      - name: test
        run: echo 1
